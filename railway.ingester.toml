[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = []
startCommand = "./nagg-ingester"
healthcheckPath = null
healthcheckTimeout = null
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
